name: C/C++ CI

on:
  pull_request:
    branches: [ "main" ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: meson setup
      run: meson setup
    - name: compile
      run: meson compile -C builddir

